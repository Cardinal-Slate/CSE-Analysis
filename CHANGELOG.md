# Changelog

## Unreleased

- Initial scaffold: the Analysis domain (ℝ) as invariants. Stacks on CSE-Field. Adds the
  transcendentals (sqrt, exp, ln, sin, cos, atan) and constants (pi, e), defined by their functional
  equations rather than by a series; completeness noted as a structural fact (a real is a bracket =
  compose(lo,hi)). Reduction (interval refinement) is a provider job. Stubs only.
